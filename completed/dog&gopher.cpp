//#include"bits/stdc++.h"
//using namespace std;
//int main(){
//    double gx, gy, dx, dy;
//    cin>>gx>>gy>>dx>>dy;
//    double x,y;
//    while(cin>>x>>y){
//        double distanceDog = sqrt(pow(dx-x, 2) + pow(dy-y, 2));
//        double distanceGop = sqrt(pow(gx-x, 2) + pow(gy-y, 2));
//        if(distanceDog > (2*distanceGop)) {
//            cout<<"The gopher can escape through the hole at ("<<x<<","<<y<<").";
//            return 0;
//        }
//    }
//    cout<<"The gopher cannot escape.";
//}